import {chromium} from 'playwright';
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1500,height:1040},deviceScaleFactor:1});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:8080');await page.waitForFunction(()=>window.__pelvis?.ready);await page.waitForTimeout(1000);await page.screenshot({path:'docs/desktop-preview.png',fullPage:true});console.log(JSON.stringify({errors,title:await page.title(),canvas:await page.locator('canvas').boundingBox()}));await browser.close();
