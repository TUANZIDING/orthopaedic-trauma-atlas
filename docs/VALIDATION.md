# 交付验证记录 · 2026-09-08

## 交付状态

已生成独立 HTML、四张 4K PNG 与可修改源码。已做本地浏览器与显示检查。**未上传 GitHub，未部署 GitHub Pages，未获得临床专家内容审定或 AO 认证。**

## 浏览器验证

通过真实 Chrome 浏览器自动交互检查（记录见 browser-check.json）：

- 两种术式 × 六个步骤可切换；四个预设视角均可用。
- 标注、风险区、骨内线段开关与透明度滑块正常。
- 点击画布可选中骨骼；来源对话框可打开并由 Escape 关闭。
- 分步定时播放、暂停正常。
- 从实际导出按钮生成四张 PNG，均为 3840 × 2160。
- 320、390、844 像素宽布局无页面横向溢出。
- `file://` 直接打开最终 HTML，阻断 HTTP/HTTPS 请求后仍可显示并切换术式。
- 无运行所需远程请求、无未捕获脚本错误。

未覆盖所有浏览器、真机多点触控、低端 GPU 或 WebGL 禁用环境；网页提供 WebGL 不可用提示。手机验证为浏览器视口模拟，不是真机测试。

## 视觉检查

已查看桌面、斜位、髂嵴及手机截图，检查骨盆和连接架取景、标注关系、中文排版和控件布局；已查看四张高清图的代表输出。修正了手机连接架贴边、股骨显示裁切和导出孤立句末标点问题。最终四张图在标点修正后由独立 HTML 重新导出。

图中保留骨骼网格的低多边形特征；高清输出增加像素分辨率，不增加源解剖几何精度。

## 教学几何检查

6 个骨骼结构、12,396 个三角形，数据数值与索引有效。检查了两种构型共 6 条中心线与对应髋骨表面的相交关系，消除了初稿中的明显中心线穿出；入口误差使用 2 mm 网格容差。详见 geometry-check.json。

这只验证**参考网格上的中心线显示一致性**，未验证螺钉完整半径、皮质厚度、骨密度、神经血管、软组织间隙或患者安全。坐标为建模参数，不是临床参数。未计算骨折复位或固定力学。

## 来源追溯

- 医学参考：AO Surgery Reference / External fixation，第 1、4–7 节。
- AO 页面由 TLS 校验启用的 curl 成功获取并核对；未向交付包复制全文、原始手术插图或视频。
- 解剖：BodyParts3D 4.0，经 human-atlas，来源提交 1c38bf35c254a891200d3cedecfd57abebe83d8d。
- BodyParts3D 官方许可页当前为 CC BY 4.0，更新日期 2025-02-27，本次在线核对。
- 完整来源、改编与许可见根目录 ATTRIBUTION.md。

校验值见 checksums.sha256。文件校验只用于确认交付字节，不表示医学审核通过。
